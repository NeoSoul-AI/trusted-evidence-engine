# TypeScript SDK Placeholder

This folder is reserved for the future TypeScript client.

TypeScript is a first-class integration language for:

- web integrations
- frontend tools
- lightweight adapters
- Node.js automation

The core engine still lives in Go.
