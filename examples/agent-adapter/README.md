# Agent Adapter Example

This folder is the placeholder for a hosted-agent or generic-agent adapter example.

A typical adapter flow:

1. Agent receives a query, claim, or prediction context
2. Adapter requests an `evidence pack`
3. Agent uses the pack as grounded input for report, opinion, or action generation
